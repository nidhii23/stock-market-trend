# ─────────────────────────────────────────────────────────────
# Stock Market Trend Prediction System
# Requirements file — install with: pip install -r requirements.txt
#
# Python version: 3.10 or 3.11 recommended (not 3.12 — nsepy issues)
# ─────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────
streamlit>=1.30.0

# ── Data ──────────────────────────────────────────────────
yfinance>=0.2.54          # NSE/BSE price data — use 0.2.54 if you get 401 errors
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
feedparser>=6.0.0         # Google News RSS for sentiment headlines

# ── NSE-specific data ─────────────────────────────────────
nsepy>=0.8.0              # NSE delivery %, historical data

# ── Machine learning ──────────────────────────────────────
torch>=2.0.0              # LSTM neural network
scikit-learn>=1.3.0       # StandardScaler, class_weight, metrics
xgboost>=2.0.0            # XGBoost ensemble

# ── NLP / Sentiment ───────────────────────────────────────
transformers==4.40.0      # FinBERT — pinned: newer versions break pipeline import
vaderSentiment>=3.3.2     # VADER fallback sentiment

# ── Technical analysis ────────────────────────────────────
ta>=0.11.0                # RSI, MACD, Bollinger Bands, ADX, EMA

# ── Visualisation ─────────────────────────────────────────
plotly>=5.0.0             # Interactive charts in Streamlit

# ── Optional: GNN (Graph Neural Network) ─────────────────
# Uncomment to enable GNN tab in the app
# Install separately AFTER torch — version must match your torch version
# See: https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
# torch-geometric>=2.0.0

# ── Optional: TFT (Temporal Fusion Transformer) ───────────
# Uncomment to enable TFT tab in the app
# pytorch-forecasting==1.6.1
# lightning>=2.0.0
# pytorch-lightning==1.9.5   # only needed if pytorch-forecasting requires it